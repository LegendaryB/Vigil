@inherits RazorSlice<IReadOnlyList<EventAction>>

<tbody id="@DashboardStyles.EventActionsTableBodyId">
@foreach (var eventAction in Model.OrderBy(a => a.Event).ThenBy(a => a.Priority))
{
    @(await RenderPartialAsync<_Row, EventAction>(eventAction))
}
</tbody>
